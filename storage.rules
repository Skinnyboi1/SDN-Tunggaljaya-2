rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can view photos (Public Read)
      allow read: if true;
      // Only authenticated operators can upload/modify images
      allow write: if request.auth != null;
    }
  }
}
